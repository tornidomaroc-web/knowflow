// THROWAWAY: deliberate type error to prove the required `tsc` check blocks a PR.
// This branch is never merged and is deleted immediately after verification.
export const gateProof: number = 'this is a string, not a number';
